spring:
  security:
    basic:
      enabled: true
    user:
      name: eureka
      password: pwdsceurekaserver

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    
